"use strict";
// 11. Names:
// Store the names of a few of your friends in an array called names. Print each person’s name by
// accessing each element in the list, one at a time.
const names = ['John', 'Jane', 'Mike', 'Sam'];
// accessing each element in the list, one at a time
names.forEach(name => {
    console.log(name);
});
